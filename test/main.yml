apiVersion: batch/v1
kind: Job
metadata:
  name: sth-job
spec:
  template:
    spec:
      containers:
      - name: sth-container
        image: "ubuntu:18.04"
        command: ["sleep",  "123123"]
        securityContext:
          privileged: true
      restartPolicy: Never
  backoffLimit: 1
